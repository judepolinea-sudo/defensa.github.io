# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables / secrets
.env
.env.*
!.env.example

# Server output
server_out.log

# One-time local admin scripts — hardcode real Firebase credentials/passwords,
# never meant to be version-controlled (see each file's own header comment)
create-admin.mjs
create-admin2.mjs
fix-admin.mjs

# Old AI conversation logs — incidental, not needed to run the app
migrated_prompt_history/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
